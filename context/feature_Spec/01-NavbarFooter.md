Read AGENTS.md for context on the project structure and goals.

### Navbar Design

Create `components/editor/navbar.tsx`.

refer image in the screenshots folder which is Navbar(BeforeScroll).png and Navbar(AfterScroll).png . Only the navbar design is required, the scroll behavior and animation will be implemented in a future feature.

Requirements:

- sticky top navbar
- color follow the `context/ui-context.md`
- add the menu inside navbar based on 'context/project-overview.md' and 'context/ui-context.md'

### Check when done

- new components compile without TypeScript errors
- no lint errors
- dialog pattern is ready for future use
